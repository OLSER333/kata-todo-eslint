добавить проверку для Min Sec чтобы чисто цифры +

при завершении задачи нельзя запускать задачу +

чтобы таймер не уходил в минус +


// я могу создать отдельный компонент для таймера,
    // туда прокидывать время, в useEffect()
            запоминать начальное время
            запускать таймер (() => {}, 1000)
                Можно внутри таймера отнимать от начального (Неизменяемого в таймере)
                текущее (Date.now()) для отображения настоящего времени
    // а в willUnmount - считывать последний Date.now()
    // возвращаться обратно, отнимать, И потом оставлять этот результат
    // А выводить уже округлённый , не знаю, будет ли точно,
    //     Но по идее должно работать


if(!done && time !== 00:00) {

    const startStamp = Date.now()
    setInterval(() => {

    })
}


getTIme(5000) --> timerRun    start = date.now()
                          setTimeout  (() => getTime(tempEnd - start))

                      NEW = stop => date.now() - start

5000 - NEW = getTime(4100)



!! нельзя уйти в минус по секнуде
    check if (new < 0 -> stopTimer)
!! нельзя run to true, when 00:00 or done +
!! дай вводить либо минуты, либо секунды, а другую (Если пустая) заполнить нулём


            timerRun--------------------->    init: false (+)
                                               if(!== 00:00)  onClick:  !curState + setInterv
            -> timerClassName
                    --
            init + change кажд раз
            при onClick

